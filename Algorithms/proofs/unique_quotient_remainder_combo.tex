\documentclass{article}
\usepackage{amsmath}
\begin{document}
	\begin{align}
	a & = qk + r \space 0 \le r < a \\
	a & = qk' + r' \space 0 \le r' < a \\
	qk + r & = qk' + r' \\
	\end{align}
\end{document}